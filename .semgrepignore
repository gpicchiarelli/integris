# Expected unsafe in Linux Landlock/seccomp adapters (IP-A-0003 / go-profile).
internal/confine/apply_linux.go

# Expected unsafe in Darwin ACL cgo adapter (IP-S-0002 / INT-IC4-0001 / go-profile).
internal/platform/acl_darwin.go

# Deterministic math/rand is intentional in planner property tests (seeded).
internal/plan/property_test.go

# Dependabot cooldown is configured; rule false-positive on nested keys.
.github/dependabot.yml

# CapGetMode is not wrapped in x/sys; the FreeBSD probe needs the out-pointer.
internal/confine/negative_capmode_freebsd.go

# FreeBSD shm_open2 is not wrapped in x/sys; sealed key FD needs the name pointer.
internal/launcher/keyfd_freebsd.go
